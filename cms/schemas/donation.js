import member from './types/member'
import date from './types/date'

const donation = {
  title: 'Donation',
  name: 'donation',
  type: 'document',
  fields: [member, date],
}

export default donation
