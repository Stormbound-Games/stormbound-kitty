const root = {
  position: 'relative',
}

const settings = {
  marginTop: '2em',
}

const puzzle = {
  position: 'absolute',
  right: 0,
  bottom: 0,
  textAlign: 'right',
  padding: '1em',
  width: 'calc((100% - 40%) / 2)',
  backgroundImage: 'linear-gradient(-194deg, transparent 60%, #00000080)',
}

const deck = {
  position: 'absolute',
  right: 0,
  top: 0,
  padding: '1em',
  width: '25%',
}

export default {
  root,
  settings,
  puzzle,
  deck,
}
