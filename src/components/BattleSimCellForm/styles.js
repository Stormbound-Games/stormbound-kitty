const button = {
  marginTop: 'auto',
}

const radios = {
  display: 'flex',
  marginTop: 'var(--s-smallest)',
  justifyContent: 'space-between',
  paddingRight: '1em',
}

const styles = { button, radios }

export default styles
