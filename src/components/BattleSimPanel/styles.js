const panel = {
  padding: 'var(--s-large)',
  position: 'relative',
  flex: '1 1 auto,',
}

const boardButton = {
  position: 'absolute',
  top: 'var(--s-large)',
  right: 'var(--s-large)',
}

const styles = { panel, boardButton }

export default styles
