const book = {
  maxWidth: '60%',
  margin: 'var(--s-larger) auto',
}

const toggle = {
  color: 'var(--beige)',
  marginBottom: 'var(--s-smaller)',
}

const styles = { book, toggle }

export default styles
