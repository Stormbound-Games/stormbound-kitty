const reset = {
  marginTop: 0,
  marginBottom: '4em',
}

const checkbox = {
  marginBottom: '1em',
  textAlign: 'left',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
}

export default { reset, checkbox }
