const rules = {
  marginTop: 0,
  paddingLeft: 0,
  listStylePosition: 'inside',
}

const item = {
  marginBottom: '0.5em',
  paddingLeft: '1.5em',
  textIndent: '-1.5em',
}

export default {
  rules,
  item,
}
