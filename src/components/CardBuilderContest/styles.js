const rules = {
  marginTop: 0,
  paddingLeft: 0,
  listStylePosition: 'inside',
}

const item = {
  marginBottom: 'var(--s-smaller)',
  paddingLeft: '1.5em',
  textIndent: '-1.5em',
}

const styles = { rules, item }

export default styles
