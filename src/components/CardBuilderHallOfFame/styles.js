const summary = { margin: 'var(--s-base) 0' }

const title = {
  margin: 0,
  fontSize: '125%',
  textTransform: 'uppercase',
}

const icon = {
  transform: 'translateY(2px) rotate(90deg)',
  marginRight: '1ch',
}

const styles = { summary, title, icon }

export default styles
