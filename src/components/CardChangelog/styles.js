const title = {
  marginBottom: 'var(--s-base)',
}

const list = {
  listStyleType: 'none',
  padding: 0,
  margin: 0,
}

const item = {
  margin: 'var(--s-smaller) 0',
}

export default {
  title,
  list,
  item,
}
