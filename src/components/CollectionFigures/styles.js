const item = {
  color: 'var(--beige)',
}

const list = {
  marginTop: 0,
  paddingLeft: '1.25em',
}

const select = {
  appearance: 'none',
  display: 'inline-block',
  width: '5ch',
  padding: 0,
  border: 0,
  borderBottom: '2px solid',
  borderRadius: 0,
}

export default {
  item,
  list,
  select,
}
