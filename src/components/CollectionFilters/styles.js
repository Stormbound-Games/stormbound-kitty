const toggle = {
  display: 'block',
  margin: '0 auto var(--s-small)',
}

const icon = {
  fontSize: '80%',
  position: 'relative',
  transform: 'translateY(2px)',
}

const cross = {
  fontSize: '120%',
}

export default {
  toggle,
  icon,
  cross,
}
