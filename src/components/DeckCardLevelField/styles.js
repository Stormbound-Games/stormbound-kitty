const field = {
  display: 'flex',
  alignItems: 'center',
  width: 'auto',

  small: {
    marginTop: 'var(--s-base)',
    order: 1,
  },
}

const label = {
  display: 'block',
  marginRight: '1em',
  flex: '1 0 auto',
}

const select = {
  width: 'auto',
}

const hint = {
  color: 'var(--white)',
  fontSize: '80%',
  display: 'block',
}

const styles = { field, label, hint, select }

export default styles
