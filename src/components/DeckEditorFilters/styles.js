const filters = {
  marginBottom: '1em',
}

const row = {
  marginBottom: 0,
  medium: { marginBottom: '0.5em' },
}

const toggle = {
  display: 'block',
  margin: '0 auto 0.75em',
}

const icon = {
  fontSize: '80%',
  position: 'relative',
  transform: 'translateY(2px)',
}

const cross = {
  fontSize: '120%',
}

export default {
  filters,
  row,
  toggle,
  icon,
  cross,
}
