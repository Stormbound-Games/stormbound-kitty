const container = {
  small: {
    padding: 0,
    marginTop: '1em',
  },
}

const trigger = {
  display: 'inline-block',
  border: '1px solid #ffffff33',
  padding: '0.1em 0.25em',
  borderRadius: '0.15em',
  cursor: 'help',
}

export default { container, trigger }
