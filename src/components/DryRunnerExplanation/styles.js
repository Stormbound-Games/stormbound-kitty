const explanation = {
  listStylePosition: 'inside',
  paddingLeft: '1em',
  marginTop: '0.5em',
}

const list = {
  marginTop: '0.5em',
  marginBottom: '1em',
  paddingLeft: '1.5em',
}

const item = {
  marginBottom: '0.2em',
  listStylePosition: 'inside',
  padding: 0,
  paddingLeft: '1.4em',
  textIndent: '-1.4em',
}

const toggle = {
  marginLeft: '1ch',
  color: 'var(--beige)',
  ':hover': { color: 'var(--white)' },
}

export default {
  explanation,
  list,
  item,
  toggle,
}
