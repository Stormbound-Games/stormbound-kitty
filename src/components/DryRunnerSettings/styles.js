const settings = {
  marginBottom: '2em',
}

const displayChance = {
  marginBottom: '1em',
  display: 'block',
}

const info = {
  color: 'var(--white)',
  display: 'block',
  textTransform: 'initial',
}

export default {
  settings,
  displayChance,
  info,
}
