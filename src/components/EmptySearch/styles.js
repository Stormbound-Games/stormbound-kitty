const container = {
  margin: '3em auto',
  textAlign: 'center',
}

const title = {
  fontSize: '200%',
  display: 'block',
  marginBottom: 'var(--s-smallest)',
}

const image = {
  display: 'block',
  margin: '0 auto 1em',
  maxWidth: '250px',
}

const copy = {
  maxWidth: '50ch',
  margin: '0 auto 2em',
}

const styles = { container, title, image, copy }

export default styles
