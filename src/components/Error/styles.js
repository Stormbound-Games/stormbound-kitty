const error = {
  margin: '3em auto',
  maxWidth: '350px',
  textAlign: 'center',
}

const title = {
  fontSize: '150%',
}

const image = {
  display: 'block',
  margin: '0 auto 1em',
  maxWidth: '250px',
}

const pre = {
  font: 'inherit',
  whiteSpace: 'pre-line',
  color: 'var(--beige)',
}

const styles = { error, title, image, pre }

export default styles
