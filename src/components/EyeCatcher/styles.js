const container = {
  display: 'flex',
  medium: { fontSize: '100%' },
}

const close = {
  marginLeft: 'auto',
  lineHeight: 1,
  display: 'none',
  paddingLeft: 'var(--s-base)',

  medium: { display: 'block' },
}

const dismiss = {
  medium: { display: 'none' },
}

const styles = { container, dismiss, close }

export default styles
