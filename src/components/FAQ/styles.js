const toc = {
  listStylePosition: 'inside',
  paddingLeft: 0,
}

const link = {
  textDecoration: 'none',
}

const list = {
  marginTop: 'var(--s-smaller)',
  marginBottom: 'var(--s-large)',
  paddingLeft: 0,
}

export default { toc, link, list }
