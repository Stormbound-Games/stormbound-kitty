const toc = {
  listStylePosition: 'inside',
  paddingLeft: 0,
}

const link = {
  textDecoration: 'none',
}

const list = {
  marginTop: '0.5em',
  marginBottom: '1.5em',
  paddingLeft: 0,
}

export default { toc, link, list }
