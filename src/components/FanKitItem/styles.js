const item = {
  position: 'relative',
}

const image = {
  display: 'block',
  margin: 'auto',
  maxWidth: '100%',
  maxHeight: '100%',
  height: 'auto',
  objectFit: 'contain',
}

const download = {
  position: 'absolute',
  bottom: '1em',
  right: '1em',
  zIndex: 10,
}

const styles = { item, image, download }

export default styles
