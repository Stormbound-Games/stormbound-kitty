const entry = {
  display: 'flex',
}

const left = {
  display: 'none',

  medium: {
    marginRight: '2ch',
    display: 'block',
  },
}

const icon = {
  position: 'relative',
  top: '0.225em',
  fontSize: '200%',
  opacity: 0.5,
  color: 'var(--beige)',
}

const main = {
  marginTop: 0,
}

const date = {
  color: 'var(--beige)',
  textTransform: 'uppercase',
  fontSize: '75%',
  letterSpacing: '1px',
}

const right = {
  marginLeft: 'auto',
  alignSelf: 'center',
  paddingLeft: '2ch',
}

export default {
  entry,
  left,
  icon,
  main,
  date,
  right,
}
