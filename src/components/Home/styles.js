// 1. The home page is supposed to be catchy, so increase its font size so text
//    is more prominent.
// 2. Position context for the absolutely-positioned news box.
const home = {
  fontSize: '125%', // 1
  position: 'relative', // 2
}

const styles = { home }

export default styles
