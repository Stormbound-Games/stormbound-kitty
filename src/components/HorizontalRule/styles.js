const rule = {
  width: '50%',
  height: '1px',
  backgroundImage:
    'linear-gradient(to right, transparent, var(--dark-beige), transparent)',
  border: 0,
  marginLeft: 'auto',
  marginRight: 'auto',
}

export default { rule }
