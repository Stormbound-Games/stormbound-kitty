const feed = {
  listStyle: 'none',
  margin: 0,
  padding: 0,
}

const item = {
  marginBottom: '2em',
}

const empty = {
  textAlign: 'center',
  maxWidth: '60ch',
  margin: '0 auto',
}

const image = {
  display: 'block',
  margin: '0 auto 1em',
  maxWidth: '250px',
}

export default { feed, item, empty, image }
