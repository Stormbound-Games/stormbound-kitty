const details = {
  textAlign: 'center',
}

const summary = {
  marginBottom: 'var(--s-base)',
}

const content = {
  textAlign: 'left',
}

const styles = { details, summary, content }

export default styles
