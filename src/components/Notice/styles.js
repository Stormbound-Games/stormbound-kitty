/**
 * 1. Artifact of the fact the info hint is usually placed at the bottom of a
 *    page to act as a call to action for the user to interact.
 */
const notice = {
  textAlign: 'center',
  fontSize: '115%',
  marginTop: '3em' /* 1 */,
}

const icon = {
  marginRight: '0.5em',
  position: 'relative',
  top: '1.5px',
}

export default { notice, icon }
