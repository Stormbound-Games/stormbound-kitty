const notice = {
  textAlign: 'center',
  fontSize: '115%',
}

const icon = {
  marginRight: 'var(--s-smaller)',
  position: 'relative',
  top: '1.5px',
}

const styles = { notice, icon }

export default styles
