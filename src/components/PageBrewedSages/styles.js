const container = {
  textAlign: 'center',
}

const cta = {
  fontSize: '120%',
  textDecoration: 'none',
  borderBottom: '1px solid',

  medium: {
    fontSize: '180%',
  },
}

const styles = { container, cta }

export default styles
