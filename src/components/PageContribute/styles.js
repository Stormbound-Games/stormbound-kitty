const cta = {
  fontSize: '120%',
  textDecoration: 'none',
  borderBottom: '1px solid',
  medium: { fontSize: '180%' },
}

const list = {
  columns: '10em',
  paddingLeft: 0,
  listStylePosition: 'inside',
}

const styles = { cta, list }

export default styles
