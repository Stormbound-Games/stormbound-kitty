// 1. Visually center the icon with the accompanying text.
const icon = {
  maxWidth: '1em',
  height: '1em',
  display: 'inline-block',
  transform: 'translateY(2px)', // 1
  margin: 0,
}

const styles = { icon }

export default styles
