// 1. Default styles from @reach/tooltip.
const tooltip = {
  zIndex: 1 /* 1 */,
  pointerEvents: 'none' /* 1 */,
  position: 'absolute' /* 1 */,
  filter: 'drop-shadow(0 1em 2em #00000033)',
}

const styles = { tooltip }

export default styles
