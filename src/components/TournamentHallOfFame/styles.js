const tournament = {
  marginBottom: '1em',
}

const name = {
  marginTop: '0 !important',
  marginBottom: 0,
}

const meta = {
  color: 'var(--beige)',
  textTransform: 'uppercase',
  fontSize: '80%',
}

export default { tournament, name, meta }
