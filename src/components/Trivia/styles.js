const form = {
  position: 'relative',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  marginBottom: '5em',
}

const legend = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  flexWrap: 'wrap',
  marginBottom: '2em',
}

const radio = {
  margin: '0.75em 1em',
}

export default { form, legend, radio }
